# Malaysian Transit Ridership Forecasting — Python dependencies
# Tested with Python 3.11 on Windows (local A100 workstation).
# Install: pip install -r requirements.txt
# PyTorch with CUDA support should be installed per https://pytorch.org/get-started/
# (the plain pip package below installs the default build for your platform).

# Core numerics & data
numpy>=1.26
pandas>=2.1
scipy>=1.11

# Machine learning
torch>=2.1
scikit-learn>=1.3
catboost>=1.2        # optional: HMT-TSF --use-catboost residual booster
shap>=0.44           # optional: HMT-TSF --shap feature importance

# Statistics (EDA)
statsmodels>=0.14
ruptures>=1.1        # change-point detection (MCO breakpoints)

# Geospatial (feature pipeline: gadm.py, population.py, osm.py)
geopandas>=0.14
shapely>=2.0
rasterio>=1.3
networkx>=3.1

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Utilities
joblib>=1.3
